{
  "name": "@reelstack/database",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "~6.19.3",
    "@reelstack/logger": "workspace:*",
    "@reelstack/types": "workspace:*"
  },
  "devDependencies": {
    "prisma": "~6.19.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
